#include "main.h"

/**
 * _abs- computes absolute value of an integer
 * @R : initializes integer values
 * Return: Always 0.
 */

int _abs(int R)
{
	if (R >= 0)
	{
		return (R);
	}
	else
	{
		return (R * -1);
	}

}


